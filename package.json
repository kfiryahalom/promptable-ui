{
  "name": "promptable-ui-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":         "pnpm --filter task-pulse dev",
    "dev:market":  "pnpm --filter market-lens dev",
    "dev:fit":     "pnpm --filter fit-board dev",
    "build":       "pnpm -r build",
    "seed":        "pnpm --filter task-pulse seed"
  }
}
